import express from "express";

// import { mobileSignIn, signIn } from "../../controller/signin/signinController";

const router = express.Router();

// router.post("/sign-in", signIn);

// router.post("/mobile-sign-in", mobileSignIn);

export default router;
